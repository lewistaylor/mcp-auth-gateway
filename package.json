{
  "name": "mcp-auth-gateway",
  "version": "1.0.0",
  "description": "Self-hosted OAuth 2.1 auth gateway for MCP servers — embeds @mcpauth/auth and proxies to internal services",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/gateway.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@mcpauth/auth": "^0.1.4",
    "better-sqlite3": "^12.9.0",
    "cookie-parser": "^1.4.7",
    "express": "^4.22.1",
    "jose": "^6.2.2",
    "kysely": "^0.28.16"
  }
}
